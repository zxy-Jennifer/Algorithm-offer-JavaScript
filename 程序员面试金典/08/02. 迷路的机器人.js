

function pathWithObstacles(obstacleGrid) {
    let res = []
    let m = obstacleGrid.length
    let n = obstacleGrid[0].length

    function recur(obstacleGrid, i, j, res) {
        if(i === m-1 && j === n-1) {
            return res
        }
        
    }
}