/**
 * 
 * @param {*} root 
 */
function findMode(root) {
    let res = []
    if (!root) {
        return res
    }

    function backTrack(node, num) {
        if (!node) {

        }
    }
}